apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort  # Can be LoadBalancer if using external access
  selector:
    app: nginx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 32080  # Optional: fixed NodePort
